package com.shopping.cart.model;

public class Enums {

    enum CouponType {
        ALL,
        NEXT,
        NTH
    }

    enum ProductType {
        WALLET,
        BACKPACK,
        TROUSER
    }

    enum DeductionType {
        PERCENTAGE,
        DOLLARS
    }
}
